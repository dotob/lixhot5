<h1>wer feiert mit:</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @guests.each do |guest| %>
  <tr>
    <td><%= guest.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'zurück', '/' %>
