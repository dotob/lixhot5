<h1>wer feiert mit:</h1>

<table>
  <tr>
    <th>name</th>
    <th>bringt mit</th>
  </tr>

<% @guests.each do |guest| %>
  <tr>
    <td><%= guest.name %></td>
    <td><%= guest.gift.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'zurück', '/' %>
